import { createAsyncThunk, createSlice } from '@reduxjs/toolkit'

export let  = createAsyncThunk(
	'/',
	async () => { 
		
	}
)

export const Slice = createSlice({
  name: '',
  initialState: {
   
  },
  reducers: {
   
  }
})

export const {  } = Slice.actions

export default Slice.reducer

import { createSlice } from '@reduxjs/toolkit'

export let  commonSlice = createSlice({
	name: 'common',

	initialState: {
	
	},

	reducers: {
		

	}
})

export let {  } = commonSlice.actions

export default commonSlice.reducer